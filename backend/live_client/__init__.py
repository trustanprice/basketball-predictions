"""NBA.com data client: client / response / endpoints / cache / lookups.

Standalone fetch layer — imports nothing from backend.win_model or backend.ratings
(see backend/AGENTS.md). Import concrete endpoint classes from
`live_client.endpoints.stats` / `live_client.endpoints.live`.
"""
