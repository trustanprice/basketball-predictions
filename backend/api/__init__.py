"""FastAPI app serving backend.win_model + backend.ratings as JSON.

Repo-root-context only (like app.py) — run via `uvicorn backend.api.main:app`
from the repo root. See backend/AGENTS.md's Imports section.
"""
