# All Python dependencies live in backend/requirements.txt.
# This pointer exists so Streamlit Cloud (which looks for requirements.txt
# at the repo root by default) keeps working without a dashboard change.
-r backend/requirements.txt
