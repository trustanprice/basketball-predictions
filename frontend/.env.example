# Base URL of the Phase 5 FastAPI backend. Server-side only (no NEXT_PUBLIC_
# prefix) — every fetch happens in a Server Component, never in the browser.
# See frontend/AGENTS.md.
API_BASE_URL=http://localhost:8000
